
지난시간에는 저장하고 저장한것을 조회하는것을 구현함
- postServiceTest에서 글 1개 조회에 조회하는 서비스를 통해서 
요청을해서 엔티티를 잘 가져오는지 확인함

= = = = =
[클라이언트에서 요구사항]
- json응답에서 title값 길이를 최대 10글자로 해주세요. 
타이틀을 10글자만 짤라서 줘야할 경우..

- 엔티티의 getter메서드를 만들때는 클래스에 서비스의 정책을 절대 넣지말 것 
- 우리는 타이틀을 10글자만 잘라서 줘야하는데 어떡할까?
1. 응답클래스를 분리하라 (서비스 정책에 맞는)
  - response > PostResponse 클래스 
  - 클라이언트 필요한 필드 id, title, content에 getter, 생성자를 만든다. 

2. 엔티티 조회한것을 변환 시켜라 
